const $target1 = document.getElementById('animationtarget1');
const $target2 = document.getElementById('animationtarget2');
const $target3 = document.getElementById('animationtarget3');
const $target4 = document.getElementById('animationtarget4');
const $target5 = document.getElementById('animationtarget5');
const $target6 = document.getElementById('animationtarget6');
const $target7 = document.getElementById('animationtarget7');
const $target8 = document.getElementById('animationtarget8');
const $target9 = document.getElementById('animationtarget9');
const $target10 = document.getElementById('animationtarget10');
const $target11 = document.getElementById('animationtarget11');
const $target12 = document.getElementById('animationtarget12');
const $target13 = document.getElementById('animationtarget13');
const $target14 = document.getElementById('animationtarget14');


document.addEventListener('scroll', function () {
  const $getElementDistance1 = $target1.getBoundingClientRect().top + $target1.
    clientHeight * 0.6;
  const $getElementDistance2 = $target2.getBoundingClientRect().top + $target2.
    clientHeight * 0.6;
  const $getElementDistance3 = $target3.getBoundingClientRect().top + $target3.
    clientHeight * 0.6;
  const $getElementDistance4 = $target4.getBoundingClientRect().top + $target4.
    clientHeight * 0.6;
  const $getElementDistance5 = $target5.getBoundingClientRect().top + $target5.
    clientHeight * 0.6;
  const $getElementDistance6 = $target6.getBoundingClientRect().top + $target6.
    clientHeight * 0.6;
  const $getElementDistance7 = $target7.getBoundingClientRect().top + $target7.
    clientHeight * 0.6;
  const $getElementDistance8 = $target8.getBoundingClientRect().top + $target8.
    clientHeight * 0.6;
  const $getElementDistance9 = $target9.getBoundingClientRect().top + $target9.
    clientHeight * 0.6;
  const $getElementDistance10 = $target10.getBoundingClientRect().top + $target10.
    clientHeight * 0.6;
  const $getElementDistance11 = $target11.getBoundingClientRect().top + $target11.
    clientHeight * 0.6;
  const $getElementDistance12 = $target12.getBoundingClientRect().top + $target12.
    clientHeight * 0.6;
  const $getElementDistance13 = $target13.getBoundingClientRect().top + $target13.
    clientHeight * 0.6;
  const $getElementDistance14 = $target14.getBoundingClientRect().top + $target14.
    clientHeight * 0.6;
  if (window.innerHeight > $getElementDistance1) {
    $target1.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance2) {
    $target2.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance3) {
    $target3.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance4) {
    $target4.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance5) {
    $target5.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance6) {
    $target6.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance7) {
    $target7.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance8) {
    $target8.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance9) {
    $target9.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance10) {
    $target10.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance11) {
    $target11.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance12) {
    $target12.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance13) {
    $target13.classList.add('show');
  }
  if (window.innerHeight > $getElementDistance14) {
    $target14.classList.add('show');
  }
})



console.log(animationtarget12)